# Copy to .env.local for local dev. In Vercel, set the same name
# under Project → Settings → Environment Variables.
#
# OAuth 2.0 "Web application" client ID from Google Cloud Console.
# Public by design — safe to expose in browser bundle. Do NOT put
# the matching client_secret anywhere near this file.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
